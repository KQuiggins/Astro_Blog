---

type Props = {
  category: string;
  
};

const { category } = Astro.props;
---

<a href={`/category/${category}`} class="rounded-3xl p-2 uppercase text-xl border-2 border-gray-200 whitespace-nowrap">
    {category}
</a>